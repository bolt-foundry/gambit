+++
label = "get_time_card"

[[actionDecks]]
name = "get_time"
path = "../decks/get_time.deck.ts"
description = "Return the current ISO timestamp."
+++

Call `get_time` when the user asks for the current time or "now." Use it once
per request; do not guess the time.
