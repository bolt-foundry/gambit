+++
label = "basic_math_card"

[[actionDecks]]
name = "basic_math"
path = "../decks/basic_math.deck.ts"
description = "Perform basic arithmetic (add, subtract, multiply, divide)."
+++

Call `basic_math` when the user asks to add, subtract, multiply, or divide two
numbers. Only call after you know both numbers and the operation (defaults to
`add` if unspecified).
