# Actions

Place action decks/cards here. Actions can be TypeScript decks (\*.deck.ts) or
markdown cards that Gambit loads alongside your root decks. Use this folder to
encapsulate tool integrations and share them across multiple decks.
