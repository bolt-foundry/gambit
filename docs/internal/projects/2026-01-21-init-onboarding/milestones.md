# Gambit Init Onboarding – Milestones

1. Init deck stub in package; REPL can run it end-to-end.
2. Init-only FS tools wired with scoped permissions.
3. `gambit init` launches init chat and writes `root.deck.md` + test deck.
4. End-to-end demo in a fresh directory with OpenRouter key.
