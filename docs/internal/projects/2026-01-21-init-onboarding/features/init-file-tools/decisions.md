# Feature Decisions – Init-Only File Tools

1. File ops limited to `write`, `exists`, `mkdir` in v1.
2. Writes scoped to the target project root.
3. Network access allowlist includes boltfoundry.com + openrouter.
4. No per-file confirmation in v1.
